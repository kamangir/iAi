from setuptools import setup

from iAi import NAME, VERSION

setup(
    name=NAME,
    author="arash@kamangir.net",
    version=VERSION,
    description="OpenAI agent for Instagram",
    packages=[NAME],
)
